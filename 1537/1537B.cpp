#include <iostream>

using namespace std;

int
main(void)
{
    uint32_t t; cin >> t;
    uint64_t n,m,j,i;
    while (t--)
    {
        cin >> n >> m >> i >> j;
        cout << 1 << " "
             << 1 << " "
             << n << " "
             << m << endl;
    }
    return 0;
}
